#include <iostream>
#include <curl/curl.h>
using namespace std;
int main() {
    CURL* curl = curl_easy_init();
    cout << "Welcome to the most bad*ss launcher in the world!!" << endl;
    return 0;
}